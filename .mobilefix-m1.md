# Mobile Fix M1 — Report

## Files Changed

### app/personnel/[id]/page.tsx
- Header `px-8 py-4` → `px-4 sm:px-8 py-3 sm:py-4`
- Header identity row outer div: added `flex-wrap gap-2` to `flex items-center justify-between` so status badge wraps rather than being squeezed
- Main content `p-8` → `p-4 sm:p-8`
- Write-Up modal outer overlay: added `p-4`
- Merit modal outer overlay: added `p-4`
- Termination modal outer overlay: added `p-4`
- Rehire modal outer overlay: added `p-4`
- (Delete modal already had `p-4`; skipped)

### app/payroll/page.tsx
- Employee card row: changed `flex items-start justify-between` → `flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2` so hours grid gets full width on mobile
- Hours grid column gap: `gap-4` → `gap-2 sm:gap-4` (avoids overflow on small screens)
- `text-right` on hours container: changed to `sm:text-right` (left-aligns on mobile)
- Approval modal outer overlay: added `p-4`
- Add Company modal outer overlay: added `p-4`

### app/overtime/page.tsx
- `<main className="flex-1 lg:ml-64">` → `<main className="flex-1">` (removed divergent left margin)
- Status filter row: `flex gap-2` → `flex flex-wrap gap-2`

### app/mileage/page.tsx
- Entries table wrapper: `overflow-hidden` → `overflow-x-auto` (table now scrolls horizontally on phones)
- Header action button row: `flex gap-2` → `flex flex-wrap gap-2`

### app/expense-report/page.tsx
- Sticky header: `p-6` → `px-4 sm:px-6 py-4`
- Bottom action bar: `flex items-center justify-between` → `flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3`

### app/call-offs/page.tsx
- No changes — stats grid already has `text-[10px] sm:text-xs` and `p-2 sm:p-4` responsive sizing; labels short enough to fit 375px

## TypeScript Check
`npx tsc --noEmit` — **clean, no errors**

## Concerns
None. All changes are pure layout/styling. No logic, handlers, state, or queries were modified.
